#include<iostream>
using namespace std;
int main()
{
  int l,b, length, area;
  cin>>l>>b;
  length = 2*(l+b);
  area = l*b;
  cout<<"Required length is "<<length<<" m"<<endl<<"Required area of carpet is "<<area<<" sqm"<<endl;
}